# These paths hold dlint's own SYNTHETIC, non-functional credential-shaped data used to exercise
# the no-secrets rule (test fixtures + the build-time core pattern examples). They are not real
# secrets — exclude them from GitHub secret-scanning alerts and push protection.
paths-ignore:
  - "tests/**"
  - "assets/secret-patterns.core.json"
